-- Auto-run by mysql image on first init.
-- Plugins will auto-CREATE-TABLE on first connect; we just need the databases.

CREATE DATABASE IF NOT EXISTS weaponpaints CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
CREATE DATABASE IF NOT EXISTS matchzy     CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
